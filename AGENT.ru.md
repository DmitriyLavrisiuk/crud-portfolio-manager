AGENTS.md по-русски: что за что отвечает
0) Основные цели

Зачем это нужно: чтобы Codex работал как “разработчик в команде”, а не как генератор кода без правил.

Security-first — безопасность важнее скорости и удобства.

Разработка версиями — делаем маленькие релизы (v0.1.0, v0.2.0…), чтобы не ломать проект и не теряться.

Чистые, удобные для ревью изменения — минимум шума в diff’ах.

1) Формат вывода (самое важное)

Зачем: ты работаешь через VS Code + git, поэтому лучше видеть diff, а не полотна кода.

По умолчанию: Codex показывает:

список изменённых файлов

и diff/patch (то есть изменения)

Полные файлы НЕ печатает, чтобы не засорять и не усложнять ревью.

Когда можно полный файл:

файл новый и маленький (до ~100 строк), или

ты прямо попросил “дай полный файл”.

В каждом ответе должно быть:

Changed files — список файлов

How to run/check — как запустить/проверить

Notes — важные замечания/допущения

2) Версии и коммиты

Зачем: чтобы проект рос контролируемо и было легко откатиться.

Работаем маленькими версиями (релизами).

1 задача = 1 коммит, по возможности.

Не смешиваем “рефакторинг ради красоты” с фичей — иначе сложно ревьюить и откатывать.

Conventional Commits

Это стандарт формата сообщений коммитов (удобно для истории и релизов).

feat: — новая функциональность

fix: — исправление бага

chore: — обслуживание/настройки/скелет

docs: — документация

refactor: — рефакторинг без изменения поведения

test: — тесты

ci: / build: — CI/сборка

Ветки и теги

Ветки: feat/..., fix/..., chore/...

Теги релизов: vX.Y.Z после завершения версии

3) Структура репозитория (монорепа)

Зачем: разделить фронт/бэк/общие типы и не мешать всё в одну кучу.

apps/web — фронтенд SPA (React)

apps/api — бэкенд API (NestJS)

packages/shared — общие типы/схемы (например Zod), чтобы фронт и бэк не расходились

4) Технологии по умолчанию

Зачем: чтобы Codex не “выдумывал” другой стек.

Web

Vite + React + TypeScript

React Router (History API)

TailwindCSS

shadcn/ui

(Позже) TanStack Query/Table, RHF+Zod

API

NestJS + TypeScript

MongoDB через Mongoose

(Позже) JWT авторизация

5) Правила безопасности (жёсткие)

Зачем: чтобы не было утечек ключей/токенов и “дыр” в архитектуре.

Секреты

Нельзя коммитить секреты вообще.

В репе только .env.example, а реальные .env в .gitignore.

Binance API keys никогда не должны попадать во фронтенд.

Никаких логов с ключами/токенами/паролями.

Пароли и токены (когда дойдём)

Пароли хэшируем argon2id

JWT:

access токен короткий

refresh токен — HttpOnly cookie

в БД хранить хеш refresh токена (не сам токен)

Шифрование в MongoDB

Важно: “шифрование базы” делаем на уровне приложения (field-level encryption).

AES-256-GCM

мастер-ключ MASTER_KEY только из env

в БД: ciphertext, iv, authTag, keyVersion

keyVersion нужен для ротации ключей

6) Ограничения Spot (на будущее, торговый модуль)

Зачем: чтобы не заложить неверную модель сделок.

На споте нет настоящего short, только если подключать margin/futures.

“Reverse/Short” нельзя сделать, если это ведёт к продаже больше, чем есть в балансе.

“Редактировать исполненный ордер” нельзя — значит редактирование = cancel + replace для открытых ордеров.

7) Стандарты кода

Зачем: стабильность и читаемость.

TypeScript строго, any избегать.

Ошибки обрабатывать единообразно, без утечки внутренних деталей.

Зависимости добавлять только если нужно, и пояснять зачем.

Линтер/форматтер должны проходить всегда.

Избегать “косметических” правок ради красоты.

8) Тестирование (поэтапно)

Зачем: не тратить время на тесты в скелете, но включить их там, где риск высокий.

v0.1.0 — может быть без тестов.

Для auth/trading/statistics — тесты на ключевую логику (когда дойдём).

9) Документация

Зачем: через 2 недели ты сам себе скажешь спасибо 🙂

Любая новая возможность = обновить README.md

.env.example должен соответствовать реальности

10) Что нельзя менять без твоего явного запроса

Зачем: чтобы Codex “не перестраивал дом” пока ты просил только окно.

API контракты после появления

правила линтинга/форматирования

структура папок

11) Порты по умолчанию

Web: 5173

API: 4000

Mongo: 27017

12) Если что-то неясно

Зачем: не тормозить прогресс вопросами.

Выбираем самое безопасное предположение

Минимальные изменения

Допущение фиксируем в Notes
